# OpenHands Agent — Состояние проекта

**Последнее обновление:** 26 июля 2026, 07:37 UTC
**Текущий статус:** TEST MODE запущен и проверен; контейнер healthy, Canvas HTTP 200, firewall активен
**Установка и запуск:** служба active (enabled), auto-start после reboot. WORK MODE не одобрен.

---

## Что завершено

| Этап | Результат |
|---|---|
| 0 — Инвентаризация | mini-server проверен, ресурсы подтверждены |
| 1 — Исследование | Agent Canvas v1.6.1 выбран, образ проверен через GHCR |
| 2 — Дизайн + консолидация | compose, systemd, firewall, validate, watchdog, supervisor — каноническая версия |

## Зафиксированные режимы

### TEST MODE — реализован в текущем deployment, запуск ещё не выполнялся

- Жёсткая сетевая и файловая изоляция.
- Пустой `test-workspace`.
- Внутренние интеграции недоступны.

### WORK MODE — спроектирован, но не реализован

- Использует тот же Agent Canvas, systemd, supervisor, контейнер и базовый deployment.
- Подключается отдельным постоянным профилем разрешений и инструментов без переписывания базового deployment.
- Постоянно предоставляет n8n, разрешённые базы Notion, выделенные папки Nextcloud, AMNESIA и разрешённые операции GitHub.
- Обычные разрешённые действия не требуют ручного подтверждения каждого шага.
- Подтверждение требуется только для массового удаления, production deployment, операций с `main`, ключами, firewall, `sudo` и system services.
- Шлюз разрешений является техническим ограничителем полномочий, а не системой подтверждения каждой операции.
- Реализуется отдельным ограниченным этапом только после успешного TEST MODE.

| 3 — TEST MODE | Установлен, запущен, проверен: healthy, перезапуск, firewall, stop/start | 26.07.2026 |
| 3.2 — LAN-доступ | UUID-профиль с WORK MODE, модель deepseek-v4-pro, браузерный диалог работает, Telegram отвечает | 26.07.2026 |

## Что НЕ выполнено

- Настройка LLM через WebUI — нет
- Тест чата, файлов, фото — нет
- Замеры CPU/RAM под нагрузкой — нет
- Реализация WORK MODE — нет

## Канонические документы

| Документ | Назначение |
|---|---|
| `docs/Решения.md` | Архитектурные решения (DEC-001–DEC-022) |
| `docs/Состояние.md` | Текущий статус (этот файл) |
| `docs/План.md` | Этапы проекта |
| `docs/Архитектура.md` | Компоненты и границы |
| `deployment/README.md` | Единственная инструкция установки |

Исследовательские документы (`00-initial-audit`, `01-platform-research`, `02-architecture-and-roadmap`, `02-deployment-design`) — архивные материалы, не инструкции запуска.

## Следующий этап

**Этап 3 — TEST MODE ✅** Установлен, запущен, проверен. Осталось: настройка LLM через WebUI и тест chat/файлы/фото.

После его успешного завершения отдельным ограниченным этапом выполняется **Этап 4 — WORK MODE**.

---

### 26 июля 2026, 09:50 UTC — Этап 3: LLM DeepSeek v4-pro настроен и проверен

**Цель:** подключить рабочую модель DeepSeek v4-pro, исправив неработающий Claude-профиль.

**Что сделано:**

| Действие | Описание |
|---|---|
| 🔧 Диагностика API-ключей | DeepSeek ✅ (HTTP 200, deepseek-v4-pro). Claude (новый) ❌ (404, ключ невалиден). DeepSeek «чат» ❌ (400, модель переименована) |
| 🔧 Исправлен профиль | `profiles/deepseek-chat.json`: `model: "deepseek/deepseek-v4-pro"` (с префиксом litellm), `base_url: "https://api.deepseek.com/v1"` |
| 🔧 Исправлен settings.json | `active_profile: "deepseek-chat"`, `llm.model: "deepseek/deepseek-v4-pro"` |
| 🔧 API-ключ | Использован существующий DeepSeek API-ключ из `/workspace/ключи токены и другая жопа/` |
| 🔄 Перезапуск | `systemctl restart openhands-agent` |

**Результаты тестов:**

| Проверка | Результат |
|---|---|
| Служба active | ✅ active (running) |
| Контейнер healthy | ✅ healthy |
| Canvas HTTP 200 | ✅ 200 |
| Профиль active | ✅ deepseek-chat |
| Модель фактическая | ✅ deepseek/deepseek-v4-pro |
| Ответ модели | ✅ «4» на запрос «2+2=? Reply in one word» (8s) |
| Thinking (reasoning) | ✅ DeepSeek v4 reasoning отображается |
| Персистентность после restart | ✅ профиль и настройки сохранились |
| CPU | 1.05% |
| RAM | 688 MB / 4 GB (16.79%) |

**Backup:**
- `/srv/openhands-agent/config/profiles/deepseek-chat.json.bak-20260726`
- `/srv/openhands-agent/config/settings.json.bak-20260726`

**Следующий технический шаг:**
Этап 3 завершён. Этап 4 — WORK MODE: постоянный профиль разрешений и инструментов.

---

### 26 июля 2026, 10:36 UTC — Этап 4A: WORK MODE реализован и проверен

**Цель:** создать рабочий профиль с ограниченным набором инструментов, отдельным workspace и файловой изоляцией.

**Что сделано:**

| Действие | Описание |
|---|---|
| 🔧 Создан профиль `work` | `agent-profiles/work.json` — 10 инструментов: `terminal`, `read_file`, `write_file`, `edit`, `list_directory`, `glob`, `grep`, `task_tool_set`, `planning_file_editor`, `canvas_ui_control` |
| 🔧 Обновлён settings.json | `active_agent_profile_id: "work-profile-0001"`, `load_memory: true`, `critic_enabled: true`, русский `system_message_suffix` с правилами WORK MODE, `confirmation_mode: false` (после проверки изоляции) |
| 🔧 Обновлён compose.yaml | Volumes: `work-workspace:/projects` (RW), `docs:/docs:ro` |
| 🔧 Обновлён prepare.sh | Создание `work-workspace/` (10001:10001, 700) и `docs/` (igor:igor, 755) |
| 📁 Созданы каталоги | `/srv/openhands-agent/work-workspace/`, `/srv/openhands-agent/docs/` (заполнен документацией) |

**Результаты проверки изоляции:**

| Проверка | Результат |
|---|---|
| /projects RW | ✅ создание, запись, чтение, удаление |
| /docs RO | ✅ чтение разрешено, запись запрещена |
| secrets/.env | ❌ недоступен |
| .ssh | ❌ недоступен |
| docker.sock | ❌ недоступен |
| /srv/amnesia, /srv/nextcloud | ❌ недоступны |
| LLM DeepSeek | ✅ active, key_set=True, 133 провайдера |
| Персистентность /projects | ✅ данные сохраняются после restart |
| Персистентность профиля | ✅ work-profile-0001 активен после restart |

**Остаточный риск (принятое ограничение Canvas):**

| Ресурс | Статус |
|---|---|
| `profiles/*.json` (API-ключи) | ⚠️ RW-доступ у агента. Архитектурное свойство Agent Canvas — LLM-ключи хранятся в JSON-профилях. Текстовый prompt не является технической защитой. |

**Backup:**
- `/srv/openhands-agent/config/settings.json.bak-20260726-4a`
- `/srv/openhands-agent/deployment/compose.yaml.bak-20260726-4a`
- `/srv/openhands-agent/deployment/scripts/prepare.sh.bak-20260726-4a`

**Процедура отката:**
```bash
sudo cp /srv/openhands-agent/config/settings.json.bak-20260726-4a /srv/openhands-agent/config/settings.json
sudo cp /srv/openhands-agent/deployment/compose.yaml.bak-20260726-4a /srv/openhands-agent/deployment/compose.yaml
sudo cp /srv/openhands-agent/deployment/scripts/prepare.sh.bak-20260726-4a /srv/openhands-agent/deployment/scripts/prepare.sh
sudo rm /srv/openhands-agent/config/agent-profiles/work.json
sudo systemctl restart openhands-agent
```

**Следующий технический шаг:**
Этап 4B — Egress для интеграций (отдельно, после подтверждения Игоря).

---

### 26 июля 2026, 13:18 UTC — Этап 5A: Host-side backup реализован

**Цель:** ежедневное резервное копирование всего проекта на внешний диск.

**Что сделано:**

| Действие | Описание |
|---|---|
| 📁 Каталог backup | `/mnt/amnesia-backup/openhands-agent/` (root:root, 700) |
| 🔗 Symlink | `/srv/openhands-agent/backups` → `/mnt/amnesia-backup/openhands-agent` |
| 🔧 Скрипт | `/usr/local/bin/openhands-backup.sh` — `tar.gz` всего проекта, исключая symlink `backups` |
| ⏱️ systemd timer | `openhands-backup.timer` — ежедневно, `RandomizedDelaySec=1800` |
| 🔒 secrets/ | `root:root`, 700. Файл `.env` — `root:root`, 600. Не монтируется в контейнер |
| 🧪 Тест | Архив 2.0 MB создан, проверен (`tar tzf`), восстановлен в `/tmp/openhands-restore-test/` — все файлы на месте |

**Результаты тестов:**

| Проверка | Результат |
|---|---|
| Архив создан | ✅ 2.0 MB (`openhands_2026-07-26.tar.gz`) |
| Еженедельная копия | ✅ `openhands_2026-W30.tar.gz` |
| Восстановление | ✅ Структура сохранена, все ключевые файлы на месте |
| symlink backups исключён | ✅ Нет рекурсии |
| Timer активен | ✅ Следующий запуск: завтра |
| Ротация | ✅ 7 daily + 4 weekly |

**Backup не запускается, если внешний диск не смонтирован** (`mountpoint -q` проверка).

**Процедура восстановления:**
```bash
sudo systemctl stop openhands-agent
sudo tar xzf /mnt/amnesia-backup/openhands-agent/daily/openhands_YYYY-MM-DD.tar.gz -C /tmp/restore/
sudo rsync -a /tmp/restore/openhands-agent/ /srv/openhands-agent/
sudo systemctl start openhands-agent
```

**Следующий технический шаг:**
Этап 5B — Healthcheck и мониторинг (существующий docker healthcheck уже работает).

---

### 26 июля 2026, 13:22 UTC — Этап 5B: Healthcheck завершён

**Цель:** мониторинг состояния агента без новых сервисов.

**Что сделано:**

| Действие | Описание |
|---|---|
| 🔧 Host-side скрипт | `/usr/local/bin/openhands-health-check.sh` — проверка systemd + healthy + Canvas 200 → journald |

**Существующие механизмы (без изменений):**

| Механизм | Статус |
|---|---|
| Docker healthcheck | 30s, `/canvas` + TCP 18000/18001, 3 retries |
| `health-watchdog.sh` | 3×unhealthy → `docker stop` → exit 1 |
| systemd | `Restart=on-failure`, `enabled` after reboot |

**Результат теста:** `OK: systemd=active container=healthy canvas=200`

**Следующий технический шаг:**
Этап 5C — Текстовый Telegram-бот через long polling.

---

### 26 июля 2026, 14:08 UTC — Этап 5C: Telegram-шлюз реализован и проверен

**Цель:** текстовый доступ к агенту через существующего бота @ProkopAsystentBot.

**Что сделано:**

| Действие | Описание |
|---|---|
| 🔑 Токен | Скопирован в `/srv/openhands-agent/secrets/telegram_bot_token` (root:root, 600) |
| 🔧 Gateway | `/usr/local/lib/openhands-tg-gateway/openhands-tg-gateway.py` — Python, long polling |
| ⚙️ systemd | `openhands-tg-gateway.service` — enabled, Restart=always |
| 🔒 Фильтр | Только user_id=587849523, только private-чат, только текст |

**Результаты тестов:**

| Проверка | Результат |
|---|---|
| Сообщение → агент → ответ | ✅ Пользователь подтвердил |
| Продолжение диалога | ✅ Тот же conversation_id |
| Перезапуск gateway | ✅ Восстанавливается |
| Журнал без токена/текста | ✅ Только START/STOP/MSG/REPLY |
| Другие пользователи | ✅ Отклоняются |
| Группы/каналы | ✅ Игнорируются |
| Остановка без Canvas | ✅ `systemctl stop openhands-tg-gateway` |

**Баг Canvas 1.6.1:** `POST /api/conversations` → 500. Обход: переиспользование существующего диалога с диска.

**Адрес Canvas:** `10.89.0.2:18000` (Docker bridge), не `10.77.0.2` (WireGuard).

**Команды:**
```bash
systemctl start openhands-tg-gateway    # запуск
systemctl stop openhands-tg-gateway     # остановка (Canvas продолжает работать)
systemctl status openhands-tg-gateway   # статус
journalctl -u openhands-tg-gateway -f   # логи
```

**Следующий технический шаг:**
Этап 5D — Голосовые сообщения (отдельно).

---

### 26 июля 2026, 15:30 UTC — Этап 3.2: LAN-доступ к рабочему Canvas исправлен и проверен ✅

**Цель:** устранить ошибку 422 `uuid_parsing` в браузерном Canvas при создании диалога и восстановить каноническую модель LLM.

**Корневые причины:**

| # | Проблема | Причина |
|---|----------|---------|
| 1 | 422 `uuid_parsing` | `active_agent_profile_id: "work-profile-0001"` — не-UUID ID, WebUI ожидал UUID |
| 2 | Нет WORK MODE у UUID-профиля | `default.json` имел `tools: null` (все инструменты), без `system_message_suffix` и `critic` |
| 3 | Ошибочная модель | Браузерный wizard записал `openai/deepseek-chat` в `agent_settings.llm.model`, перекрывая профиль |
| 4 | Неканоническая модель | Профиль содержал `deepseek/deepseek-chat` вместо подтверждённой `deepseek/deepseek-v4-pro` |

**Что сделано:**

| Действие | Файл | Описание |
|---|---|---|
| 🔧 UUID-профиль с WORK MODE | `agent-profiles/default.json` | `id: 2e07efc4-...` (сохранён), +10 инструментов, +RU system_message_suffix, +`critic_enabled: true` |
| 🔧 Каноническая модель | `profiles/deepseek-chat.json` | `model: "deepseek/deepseek-v4-pro"` |
| 🔧 UUID + модель | `settings.json` | `active_agent_profile_id` → UUID; `llm.model` → `deepseek/deepseek-v4-pro` |
| 🔒 API-ключ | Без изменений | Серверная передача через `LOCAL_BACKEND_API_KEY`, не в браузере |

**Доказательства:**

| Проверка | Результат |
|---|---|
| Браузерный диалог | `7fc4057a70c44649b1da83c53eda8977` — создан без 422 |
| Ответ модели | «4» на «Сколько будет 2+2?» |
| Модель в метаданных | `deepseek/deepseek-v4-pro` |
| Контейнер | `running / healthy` |
| Личная проверка Игоря (браузер) | ✅ Canvas открыт, диалог создан, агент ответил |
| Личная проверка Игоря (Telegram) | ✅ Бот отвечает после исправления |

**Backup:** созданы `.bak-20260726-1510` для всех изменённых файлов на mini-server.

**Конфигурация не в Git:** `settings.json`, `profiles/` и `agent-profiles/` — runtime-артефакты на mini-server (DEC-015). Изменения зафиксированы только в документации.
