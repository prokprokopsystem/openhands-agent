# OpenHands Agent — Состояние проекта

**Последнее обновление:** 25 июля 2026, 22:30 CEST  
**Текущий этап:** готовность к первому изолированному тестовому запуску  

---

## Что завершено

| Этап | Результат | Дата |
|---|---|---|
| 0 — Инвентаризация | mini-server проверен, ресурсы подтверждены | 25.07.2026 |
| 1 — Исследование платформы | Agent Canvas v1.6.1 выбран, образ проверен через GHCR | 25.07.2026 |
| 2 — Дизайн развёртывания | compose.yaml, скрипты, egress-политика подготовлены | 25.07.2026 |

## Что НЕ выполнено

- Установка на mini-server — **не выполнялась**
- Первый тестовый запуск — **не выполнялся**
- Docker pull / compose up — **не выполнялись**
- Systemd unit — подготовлен, **не установлен**
- Egress-правила — подготовлены, **не применены**
- Публичный домен — **не настроен**

## Текущее состояние

- Репозиторий: https://github.com/prokprokopsystem/openhands-agent
- Версия: Agent Canvas `v1.6.1` (digest `sha256:fc24163754bee...`)
- Порт: `10.77.0.2:8000` (WG-only)
- Авторизация: `LOCAL_BACKEND_API_KEY` (передаётся автоматически, без экрана ввода)
- Docker socket: не используется
- Изоляция: отдельная сеть `10.89.0.0/28`, test-workspace, egress-правила

## Следующий этап

**Этап 3 — первый изолированный тестовый запуск:** подготовить каталоги на mini-server, запустить контейнер, проверить WebUI, авторизацию, файлы, остановку, перезапуск, потребление ресурсов и egress-изоляцию. Только с пустым test-workspace. Без GitHub, SSH, n8n, Nextcloud, Notion.

---

*Правило обновления: изменение → проверка → обновление этого файла → commit.*
